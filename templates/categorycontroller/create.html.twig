{% extends 'base.html.twig' %}

{% block title %}Create Category{% endblock %}

{% block body %}
    <h1>Create a New Category</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button class="btn btn-success">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('app_categorycontroller') }}">⬅ Back to list</a>
{% endblock %}

